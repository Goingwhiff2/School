total = 0

for i in range(5):
    num = int(input("Enter a number: "))
    include = input("Do you want to include this number? (y/n): ").lower()
    
    if include == 'y':
        total += num

print(f"Total: {total}")
